service: default
runtime: python37
automatic_scaling:
  min_instances: 0
  max_instances: 1

handlers:
- url: /
  static_files: build/index.html
  upload: build/index.html
  
- url: /(.*)
  static_files: build/\1
  upload: build/(.*)
